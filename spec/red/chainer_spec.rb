require "spec_helper"

RSpec.describe Red::Chainer do
  it "has a version number" do
    expect(Red::Chainer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
